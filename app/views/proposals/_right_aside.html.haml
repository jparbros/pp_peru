%aside#sidebar.span3
  .widget
    %h4.widget-title Busqueda
    .widget-content
      %form.search-form{:action => "#{propuestas_path}", :method => "get"}
        %input{"data-prompt" => "Buscar...", :name => "query", :type => "text", :value => "Buscar ..."}
          %button{:name => "submit", :type => "submit"} Buscar

  .widget
    %h4.widget-title Temas
    .widget-content
      %ul.list.tags
        - Topic.all.each do |topic|
          %li
            = link_to topic.name, propuestas_path(topic_id: topic.id)