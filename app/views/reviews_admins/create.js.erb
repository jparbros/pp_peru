var annotation_id = "<%=@annotation.id%>";
$("#reviews_by_admin-" + annotation_id).hide();
$("#report_annotation-" + annotation_id).hide();
$("li#"+ annotation_id + " .alert").removeClass('hidden');
$("li#"+ annotation_id + " .alert").text('Los cambios se han guardado correctamente');