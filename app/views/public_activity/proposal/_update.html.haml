- if @user == @current_user and @user == a.owner
	Usted a publicado la siguiente propuesta:
- else
	= link_to a.owner.user_name, usuario_path(a.owner)
	a publicado la siguiente propuesta
= link_to p[:title], propuesta_path(p[:id])